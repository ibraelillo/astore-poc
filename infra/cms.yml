version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - nvm use 14
            - node -v
            - yarn install
        build:
          commands:
            - nvm use 14
            - node -v
            - yarn run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: packages/frontends/cms
